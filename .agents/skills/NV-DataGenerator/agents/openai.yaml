interface:
  display_name: "NV DataGenerator"
  short_description: "Repo-specific patterns and workflows for NV-DataGenerator"
  default_prompt: "Use the NV-DataGenerator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true